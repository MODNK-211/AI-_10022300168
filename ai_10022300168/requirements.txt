# ── Core RAG dependencies ─────────────────────────────────────────────────────
# All RAG components (chunking, embedding, vector store, retrieval, prompting)
# are implemented MANUALLY using only these allowed libraries.

pandas>=2.0.0
numpy>=1.24.0
sentence-transformers>=2.7.0
faiss-cpu>=1.7.4
PyPDF2>=3.0.0
scikit-learn>=1.3.0
streamlit>=1.32.0
requests>=2.31.0

# ── Dev / test ────────────────────────────────────────────────────────────────
pytest>=7.4.0
python-dotenv>=1.0.0
